using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Thyra : MonoBehaviour
{/*Description Notes:
  * -defense player
  * -low level magic "block" generators and enemy alike
  * -can only carry no more than two of potions at a time
  * -the oppisite of Thor just less
  * -can pickup both, but more keys
  */
}
